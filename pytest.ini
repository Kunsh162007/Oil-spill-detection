[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers --basetemp=.pytest-tmp
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: runs the full pipeline; excluded with -m "not slow"
