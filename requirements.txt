# ---------------------------------------------------------------------------
# There are four dependency files, split by WHERE the code runs, not by taste:
#
#   requirements.txt           development: everything a laptop needs to run
#                              the pipeline, the tests and the eval scripts.
#   requirements-api.txt       the deployed container ONLY. A strict subset -
#                              see the note in that file for why.
#   requirements-ml.txt        torch and friends. Training only, ~2.5 GB, and
#                              nothing at serve time imports it.
#   requirements-optional.txt  heavy or credentialed extras. The pipeline
#                              degrades gracefully when they are absent.
# ---------------------------------------------------------------------------
# Core numerics
numpy>=1.26,<3
scipy>=1.11
scikit-learn>=1.3
scikit-image>=0.22
pandas>=2.0

# Geospatial
shapely>=2.0
rasterio>=1.3
pyproj>=3.6

# Imaging
opencv-python-headless>=4.8
pillow>=10.0

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2

# Config / IO
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31
orjson>=3.9

# Plotting (bakeoff scatter, eval figures)
matplotlib>=3.8

# Tests
pytest>=8.0
pytest-cov>=4.1
httpx>=0.27

# Ocean currents for the drift stage. Development-only: the API
# container never reads currents - drift runs at precompute time and
# only its result is served, so this stays out of requirements-api.txt.
copernicusmarine>=2.0        # CMEMS subsetting; needs `copernicusmarine login`
xarray>=2024.0               # reads the CMEMS/ERA5 NetCDF fields
